post_install() {
    echo "Running depmod for ${1}-ARCH"
    depmod "${1}-ARCH"
    echo --------------------------------------------------------
    echo Please ensure to reboot your system in order for the
    echo patched kernel module to get loaded.
    echo --------------------------------------------------------
}

post_upgrade () {
    post_install $1
}
